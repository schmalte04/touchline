{
  "name": "csv-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for CSV file access and querying",
  "type": "module",
  "bin": {
    "csv-mcp-server": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "start": "npm run build && node build/index.js",
    "dev": "tsc && node build/index.js"
  },
  "keywords": ["mcp", "csv", "server", "claude"],
  "author": "",
  "license": "ISC",
  "files": ["build"],
  "devDependencies": {
    "@types/node": "^24.0.4",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.1",
    "csv-parser": "^3.2.0",
    "zod": "^3.25.67"
  }
}
